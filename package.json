{
  "name": "cypress_poc-master",
  "version": "1.0.0",
  "description": "install cypress --save-dev # start Cypress: npx cypress open",
  "main": "index.js",
  "dependencies": {
    "cypress-cucumber-preprocessor": "^2.3.1"
  },
  "devDependencies": {
    "cypress": "^7.0.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
